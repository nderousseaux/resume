{
  "dependencies": {
    "jsonresume-theme-stackoverflow": "https://github.com/nderousseaux/jsonresume-theme-stackoverflow-french.git",
    "resume-cli": "^3.1.2"
  },
  "scripts": {
    "html": "for file in resumes/*.json; do resume export \"${file%.json}.html\" --resume \"$file\" --theme stackoverflow; done",
    "pdf": "for file in resumes/*.json; do resume export \"${file%.json}.pdf\" --resume \"$file\" --theme stackoverflow; done"
  }
}
