{
  "dependencies": {
    "jsonresume-theme-stackoverflow": "https://github.com/nderousseaux/jsonresume-theme-stackoverflow-french.git",
    "resume-cli": "^3.1.2"
  },
  "scripts": {
    "build": "for file in resumes/*.json; do resume export \"${file%.json}.html\" --resume \"$file\" --theme stackoverflow; done"
  }
}
